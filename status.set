#!/bin/sh
. ./vkmethod.sh .				# импорт метода вызова vk api
s="&text=$(echo $* | tr ' ' '+')" 		# все аргументы превращаются в строку
exit $(vk_method "status.set" $s > /dev/null)	# информацию про успех съесть (если будет ошибка, скорее всего vk_method также вернёт ошибку)

